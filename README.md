# remove_multicollinearity_by_condition_number
Function that leverages the condition number (CN) of a correlation matrix     of features, the correlation matrix of the features, and removes all     those that are above a user-defined threshold (CN).
